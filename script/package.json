{
    "license": "BSD-3-Clause",
    "devDependencies": {
        "elm-codegen": "^0.6.3",
        "elm-pages": "^3.3.4"
    },
    "scripts": {
        "start": "bunx elm-pages run src/Main.elm",
        "watch": "watchexec -c -w elm-build/src -w src -w ../DLCs -w ../media 'bun start'"
    }
}
